import { Link } from 'react-router-dom';
import {useDispatch} from 'react-redux';

import PlayPause from './PlayPause';
import {playPause, setActive} from 'redux/f';

const SongCard = () => (
  <div>SongCard</div>
);

export default SongCard;
